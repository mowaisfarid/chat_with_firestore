module.exports = {
  root: true, // Indicates this is the root configuration
  extends: ['@react-native-community'], // Use React Native's recommended ESLint configuration
  rules: {
    'no-console': 'warn', // Example: Warn when console statements are used
    'prettier/prettier': ['error'], // Example: Enforce Prettier rules as errors
  },
};
